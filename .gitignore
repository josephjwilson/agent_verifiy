__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
.venv/
# Model weights stay out of git — the HF cache lives at /workspace/hf-cache.
# outputs/ IS tracked: the committed run artifacts are what make `verify` reproducible
# from a clone, with no GPU and no model download.
cache/
*.pt
*.safetensors
